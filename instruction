terraform init
terraform plan -var-file=aws.tfvars
terraform apply -var-file=aws.tfvars -auto-approve
if asked update
run cmd terraform init -upgrade
terraform destroy -var-file=aws.tfvars -auto-approve
